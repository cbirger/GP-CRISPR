sgRNA #1 prefix:  CACCACCG
sgRNA #2 prefix:  GTTTTAGA

Other input parameters:

reads file = one of the fastq.gz files

prefix mismatches = CACCACCG if foward reads file, GTTTTAGA if reverse
reads file.

sgRNA length = 20

max num reads = ALL

output basename = <reads.file_basename>.TRIMMED

reverse reads = true/false, depending on whether trimming reverse reads
file or not.


